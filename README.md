./lab/lab.js is the modular main entry point. Can include all assets (css, img, svg, etc).
./lab-client/dist/index.html is the client that uses 'common' assets and only 'button' assets (eg. no input component)
